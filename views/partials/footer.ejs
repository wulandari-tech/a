                        </div>
                    </main>
                </div>
            </div>
    </div> 

    <footer class="footer text-center">
        <div class="container">
            <p class="mb-1">© <%= new Date().getFullYear() %> <%= storeName %>. all rights reserved.</p>
            <p class="mb-1">
                <i class="fab fa-whatsapp"></i> <a href="<%= storeWhatsappLink %>" target="_blank"><%= storePhoneNumber %></a>
                <span class="mx-2">|</span>
                <i class="fab fa-instagram"></i> <a href="https://instagram.com/qoupay_indonesia" target="_blank">@qoupay_indonesia</a>
            </p>
        </div>
    </footer>
    
    <% if (!(pageTitle && (pageTitle.toLowerCase().includes('live chat support') || pageTitle.toLowerCase().includes('admin dashboard')) )) { %>
        <div id="liveChatToggleContainer">
            <button id="chatToggleButton" class="btn btn-primary rounded-circle p-0" type="button" data-bs-toggle="modal" data-bs-target="#liveChatModal">
                <i class="fas fa-comments"></i>
            </button>
        </div>

        <div class="modal fade" id="liveChatModal" tabindex="-1" aria-labelledby="liveChatModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-scrollable modal-dialog-centered">
                <div class="modal-content" id="chatModalContent">
                    <div class="modal-header bg-primary text-white">
                        <h5 class="modal-title" id="liveChatModalLabel"><i class="fas fa-headset me-2"></i>Live Chat Support</h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body p-0">
                        <div id="chatMessages" class="p-3">
                        </div>
                    </div>
                    <div class="modal-footer">
                        <form id="chatForm" class="w-100 d-flex">
                            <input type="text" id="chatInput" class="form-control me-2" placeholder="Ketik pesan Anda...">
                            <button type="submit" class="btn btn-success"><i class="fas fa-paper-plane"></i></button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    <% } %>


    <% if (currentUser) { %>
    <script>
        document.body.dataset.currentUser = JSON.stringify({
            username: "<%= currentUser.username %>",
            id: "<%= currentUser._id %>"
        });
    </script>
    <% } %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/autoloader/prism-autoloader.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/line-numbers/prism-line-numbers.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/script.js"></script>
    <% if (!(pageTitle && (pageTitle.toLowerCase().includes('live chat support') || pageTitle.toLowerCase().includes('admin dashboard')) )) { %>
        <script src="/js/chat.js"></script>
    <% } %>
    <script>
        document.addEventListener('DOMContentLoaded', (event) => {
            if (typeof Prism !== 'undefined') {
                Prism.highlightAll();
            }
            if (typeof AOS !== 'undefined') {
                AOS.init({
                    duration: 700,
                    once: true,
                    offset: 30,
                    easing: 'ease-in-out-quad'
                });
            }
            const alertContainer = document.querySelector('.alert-container');
            if(alertContainer && alertContainer.children.length > 0){
                setTimeout(() => {
                    let firstChild = alertContainer.firstChild;
                    while(firstChild){
                        alertContainer.removeChild(firstChild);
                        firstChild = alertContainer.firstChild;
                    }
                }, 7000);
            }
        });
    </script>
</body>
</html>